import { createAsyncThunk } from '@reduxjs/toolkit';
import { projectService } from '@api/services/project.service';

/*
export const setFilter = createAsyncThunk('navigation/setFilter', async (filter: any) => {
  console.log(filter);
  const response = await projectService.setFilter(filter);
  return response;
});
*/
